#端口
server:
  port: 8083
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: user
  main:
    allow-bean-definition-overriding: true

#是否开启url权限控制
enable:
  auth: true

#权限不校验的url,及不受Security保护的url
auth:
  filter:
    url: /,/login,/register,/logout,**/oauth,/favicon.ico